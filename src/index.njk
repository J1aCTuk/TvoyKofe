<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Моя таблица</title>
  {% set css %}
    {% include "style.css" %}
  {% endset %}
  <style>
    {{ css | cssmin | safe }}
  </style>
</head>
<body>
  <div class="tint">
    <header>
      <h1>Привет Trello!</h1>
      </header>
  </div>
  <main>
    {% for card in trello %}
    <section>
      <h2>{{ card.name }}</h2>
      <div>
      {% markdown %}
        {{- card.desc | safe }}
      {% endmarkdown %}
      </div>
    </section>
    {% endfor %}
  </main>
  <div class="tint">
    <footer>
    </footer>
  </div>
</body>
</html>
